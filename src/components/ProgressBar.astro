<script>
  interface Props {
    percent: number;
    status: string;
    visible: boolean;
  }

  const { percent, status, visible } = Astro.props;
</script>

<style>
  .progress-wrapper {
    margin-top: 18px;
    display: none;
  }

  .progress-wrapper.active {
    display: block;
  }

  .progress-bar {
    width: 100%;
    height: 4px;
    background: var(--bg-tertiary);
    border-radius: 2px;
    overflow: hidden;
  }

  .progress-bar .fill {
    height: 100%;
    background: var(--primary);
    border-radius: 2px;
    width: 0%;
    transition: width 0.3s ease;
  }

  .progress-text {
    display: flex;
    justify-content: space-between;
    font-size: 12px;
    color: var(--text-muted);
    margin-top: 6px;
  }
</style>

<div class={`progress-wrapper ${visible ? 'active' : ''}`}>
  <div class="progress-bar">
    <div class="fill" style="width: ${Math.min(percent, 100)}%"></div>
  </div>
  <div class="progress-text">
    <span>{Math.min(percent, 100)}%</span>
    <span>{status}</span>
  </div>
</div>
